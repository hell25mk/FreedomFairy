#include "Define.h"
#include "DxLib.h"

const int Define::Color::Color_White = GetColor(255, 255, 255);
const int Define::Color::Color_Red = GetColor(255, 0, 0);
const int Define::Color::Color_Green = GetColor(0, 255, 0);
const int Define::Color::Color_Blue = GetColor(0, 0, 255);